<% @title = '制作物登録' %>
<h1><%= @title %></h1>
<%= form_with model: @portfolio, url: :admin_portfolios do |f| %>
  <%= render 'form', f: f %>
  <div class="buttons">
    <%= f.submit '登録' %>
    <%= link_to  "キャンセル", :admin_root %>
  </div>
<% end %>
